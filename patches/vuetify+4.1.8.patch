diff --git a/node_modules/vuetify/lib/components/VBadge/VBadge.js b/node_modules/vuetify/lib/components/VBadge/VBadge.js
index bd68cb2..8745c92 100644
--- a/node_modules/vuetify/lib/components/VBadge/VBadge.js
+++ b/node_modules/vuetify/lib/components/VBadge/VBadge.js
@@ -41,7 +41,7 @@ export const makeVBadgeProps = propsFactory({
     location: 'top end'
   }),
   ...makeRoundedProps(),
-  ...makeTagProps(),
+  ...makeTagProps({ tag: 'span' }),
   ...makeThemeProps(),
   ...makeTransitionProps({
     transition: 'scale-rotate-transition'
@@ -94,7 +94,7 @@ export const VBadge = genericComponent()({
       }, attrs, {
         "style": props.style
       }), {
-        default: () => [_createElementVNode("div", {
+        default: () => [_createElementVNode("span", {
           "class": "v-badge__wrapper"
         }, [ctx.slots.default?.(), _createVNode(MaybeTransition, {
           "transition": props.transition
diff --git a/node_modules/vuetify/lib/components/VBtn/VBtn.js b/node_modules/vuetify/lib/components/VBtn/VBtn.js
index fa5d137..5389311 100644
--- a/node_modules/vuetify/lib/components/VBtn/VBtn.js
+++ b/node_modules/vuetify/lib/components/VBtn/VBtn.js
@@ -183,7 +183,8 @@ export const VBtn = genericComponent()({
         "disabled": isDisabled.value && Tag !== 'a' || undefined,
         "tabindex": props.loading || props.readonly ? -1 : undefined,
         "onClick": onClick,
-        "value": valueAttr.value
+        // `value` is invalid on <a>: only emit it when the button is a real button (W3C)
+        "value": Tag === 'a' ? undefined : valueAttr.value
       }), {
         default: () => [genOverlays(true, 'v-btn'), !props.icon && hasPrepend && _createElementVNode("span", {
           "key": "prepend",
diff --git a/node_modules/vuetify/lib/components/VChip/VChip.js b/node_modules/vuetify/lib/components/VChip/VChip.js
index 2ec5103..77e42b7 100644
--- a/node_modules/vuetify/lib/components/VChip/VChip.js
+++ b/node_modules/vuetify/lib/components/VChip/VChip.js
@@ -199,7 +199,7 @@ export const VChip = genericComponent()({
         default: () => [genOverlays(isClickable.value, 'v-chip'), hasFilter && _createVNode(VExpandXTransition, {
           "key": "filter"
         }, {
-          default: () => [_withDirectives(_createElementVNode("div", {
+          default: () => [_withDirectives(_createElementVNode("span", {
             "class": "v-chip__filter"
           }, [!slots.filter ? _createVNode(VIcon, {
             "key": "filter-icon",
@@ -213,7 +213,7 @@ export const VChip = genericComponent()({
               }
             }
           }, slots.filter)]), [[_vShow, group.isSelected.value]])]
-        }), hasPrepend && _createElementVNode("div", {
+        }), hasPrepend && _createElementVNode("span", {
           "key": "prepend",
           "class": "v-chip__prepend"
         }, [!slots.prepend ? _createElementVNode(_Fragment, null, [props.prependIcon && _createVNode(VIcon, {
@@ -237,7 +237,7 @@ export const VChip = genericComponent()({
               start: true
             }
           }
-        }, slots.prepend)]), _createElementVNode("div", {
+        }, slots.prepend)]), _createElementVNode("span", {
           "class": "v-chip__content",
           "data-no-activator": ""
         }, [slots.default?.({
@@ -247,7 +247,7 @@ export const VChip = genericComponent()({
           toggle: group?.toggle,
           value: group?.value.value,
           disabled: props.disabled
-        }) ?? toDisplayString(props.text)]), hasAppend && _createElementVNode("div", {
+        }) ?? toDisplayString(props.text)]), hasAppend && _createElementVNode("span", {
           "key": "append",
           "class": "v-chip__append"
         }, [!slots.append ? _createElementVNode(_Fragment, null, [props.appendIcon && _createVNode(VIcon, {
diff --git a/node_modules/vuetify/lib/components/VDivider/VDivider.js b/node_modules/vuetify/lib/components/VDivider/VDivider.js
index 0ac9ecf..e7388f2 100644
--- a/node_modules/vuetify/lib/components/VDivider/VDivider.js
+++ b/node_modules/vuetify/lib/components/VDivider/VDivider.js
@@ -73,7 +73,10 @@ export const VDivider = genericComponent()({
           'border-style': props.variant
         }, props.style]),
         "aria-orientation": !attrs.role || attrs.role === 'separator' ? props.vertical ? 'vertical' : 'horizontal' : undefined,
-        "role": `${attrs.role || 'separator'}`
+        // <hr> already carries an implicit separator role: emitting it again is
+        // redundant and reported by the W3C validator. An explicit override
+        // (role="presentation") is still honoured.
+        "role": attrs.role
       }, null);
       if (!slots.default) return divider;
       return _createElementVNode("div", {
diff --git a/node_modules/vuetify/lib/components/VField/VField.js b/node_modules/vuetify/lib/components/VField/VField.js
index 5b75eb8..bef9f28 100644
--- a/node_modules/vuetify/lib/components/VField/VField.js
+++ b/node_modules/vuetify/lib/components/VField/VField.js
@@ -183,7 +183,9 @@ export const VField = genericComponent()({
     const mainLabelProps = toRef(() => {
       const ariaHidden = hasFloatingLabel.value && isActive.value;
       return {
-        'aria-hidden': ariaHidden,
+        // only emit the attribute when it is actually true: aria-hidden is forbidden
+        // on a <label> associated with a labelable element, whatever its value
+        'aria-hidden': ariaHidden || undefined,
         for: ariaHidden ? undefined : id.value
       };
     });
diff --git a/node_modules/vuetify/lib/components/VSelect/VSelect.js b/node_modules/vuetify/lib/components/VSelect/VSelect.js
index dd26eca..6fbbc64 100644
--- a/node_modules/vuetify/lib/components/VSelect/VSelect.js
+++ b/node_modules/vuetify/lib/components/VSelect/VSelect.js
@@ -436,6 +436,7 @@ export const VSelect = genericComponent()({
         }, [items.value.map(item => _createElementVNode("option", {
           "key": item.value,
           "value": item.value,
+          "label": item.title,
           "selected": selectedValues.value.includes(item.value)
         }, null))]), _createVNode(VMenu, _mergeProps({
           "id": menuId.value,
diff --git a/node_modules/vuetify/lib/components/VTabs/VTab.js b/node_modules/vuetify/lib/components/VTabs/VTab.js
index d3fcfdf..8bc36cf 100644
--- a/node_modules/vuetify/lib/components/VTabs/VTab.js
+++ b/node_modules/vuetify/lib/components/VTabs/VTab.js
@@ -122,7 +122,7 @@ export const VTab = genericComponent()({
         "onGroup:selected": updateSlider
       }), {
         ...slots,
-        default: () => _createElementVNode(_Fragment, null, [slots.default?.() ?? props.text, !props.hideSlider && _createElementVNode("div", {
+        default: () => _createElementVNode(_Fragment, null, [slots.default?.() ?? props.text, !props.hideSlider && _createElementVNode("span", {
           "ref": sliderEl,
           "class": _normalizeClass(['v-tab__slider', props.inset ? insetColorClasses.value : sliderColorClasses.value]),
           "style": _normalizeStyle([sliderColorStyles.value, props.inset ? insetColorStyles.value : sliderColorClasses.value])
diff --git a/node_modules/vuetify/lib/composables/router.js b/node_modules/vuetify/lib/composables/router.js
index db5aad7..bb175d7 100644
--- a/node_modules/vuetify/lib/composables/router.js
+++ b/node_modules/vuetify/lib/composables/router.js
@@ -1,6 +1,8 @@
 // Utilities
 import { computed, nextTick, onScopeDispose, reactive, resolveDynamicComponent, toRef } from 'vue';
 import { deepEqual, getCurrentInstance, hasEvent, IN_BROWSER, propsFactory } from "../util/index.js"; // Types
+import useParentUrl from '@data-fair/frame/lib/vue-router/use-parent-url.js'
+
 export function useRoute() {
   const vm = getCurrentInstance('useRoute');
   return computed(() => vm?.proxy?.$route);
@@ -43,7 +45,12 @@ export function useLink(props, attrs) {
     if (!route.value) return link.value.isExactActive?.value ?? false;
     return link.value.isExactActive?.value && deepEqual(link.value.route.value.query, route.value.query);
   });
-  const href = computed(() => props.to ? link.value?.route.value.href : props.href);
+  // patch this composable so that the links work in iframe
+  const parentUrl = useParentUrl(() => props.to, useRouter())
+  const href = computed(() => props.to ? parentUrl.value : props.href);
+  // Only emit `target` when the element is actually rendered as a link (<a>): Vuetify spreads linkProps
+  // on whatever tag is rendered (button, span…), and `target` on non-link tags is invalid HTML (W3C).
+  const target = computed(() => isLink.value ? (props.target ?? attrs.target ?? '_top') : undefined)
   const isRouterLink = toRef(() => !!props.to);
   return {
     isLink,
@@ -55,6 +62,7 @@ export function useLink(props, attrs) {
     href,
     linkProps: reactive({
       href,
+      target,
       'aria-current': toRef(() => isActive.value ? 'page' : undefined),
       'aria-disabled': toRef(() => props.disabled && isLink.value ? 'true' : undefined),
       tabindex: toRef(() => props.disabled && isLink.value ? '-1' : undefined)
diff --git a/node_modules/vuetify/lib/composables/theme.js b/node_modules/vuetify/lib/composables/theme.js
index e61b99d..44dd5a7 100644
--- a/node_modules/vuetify/lib/composables/theme.js
+++ b/node_modules/vuetify/lib/composables/theme.js
@@ -282,10 +282,11 @@ export function createTheme(options) {
       function getHead() {
         return {
           style: [{
-            textContent: styles.value,
+            // Pre-declare canonical layer order so the theme stylesheet can stay in <head>
+            // (HTML-valid) without depending on parse order vs. main.css.
+            textContent: '@layer vuetify-core, vuetify-components, vuetify-overrides, vuetify-utilities, vuetify-final;\n' + styles.value,
             id: parsedOptions.stylesheetId,
-            nonce: parsedOptions.cspNonce || false,
-            tagPosition: 'bodyOpen'
+            nonce: parsedOptions.cspNonce || false
           }]
         };
       }
